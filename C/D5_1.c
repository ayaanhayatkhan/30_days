#include <stdio.h>

int main() {
    
    // declaring and initializing a string
    char str[] = "Ayaan";

    // printing the string
    printf("The string is: %s\n", str);
    printf("The last character of array is :%c\n ",str[5]);

    return 0;
}