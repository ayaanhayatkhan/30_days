#include <stdio.h>

int main()
{

    // Use sizeof() to know size of the data types
    printf("The size of int: %d\n", sizeof(int));
    printf("The size of char: %d\n", sizeof(char));
    printf("The size of float: %d\n", sizeof(float));
    printf("The size of double: %d\n", sizeof(double));
   // printf("The size of memory: %d\n",sizeof(memory));
    printf("The largest memory in these all is of Double\n");
    return 0;
}